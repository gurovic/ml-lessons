{
  "title": "Булева индексация — магия numpy внутри pandas",
  "bullets": [
    "`df[df['age'] > 30]` — маска numpy внутри pandas",
    "Шаг 1: `df['age'] > 30` → булев массив `[True, False, True, \\ldots]`",
    "Шаг 2: `df[mask]` возвращает строки, где `True` — прямое наследие numpy",
    "Составные условия: `&`, `|`, `~` (не `and`/`or`); **каждое условие в скобках**"
  ],
  "notes": "Показать mask.sum() и ошибку без скобок.",
  "visuals": [
    {
      "description": "Схема 3 шага: столбец age → булева маска (зелёные/красные строки) → отфильтрованный DataFrame. Подписи на русском.",
      "output": "boolean_mask_flow.png"
    }
  ],
  "notebook": {
    "include": true,
    "kinds": [
      "example"
    ],
    "hint": "булева маска, составные условия со скобками"
  }
}
